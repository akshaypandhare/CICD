name: Deploy Pipeline

on:
  push:
    branches:
      - main

jobs:
  docker-build-and-push:
    name: Build, Scan, and Push Docker Image
    runs-on: ubuntu-latest
    steps:
      # Checkout the code
      - name: Checkout Code
        uses: actions/checkout@v3
      # Checkout the code
      - name: commit time and date
        run: |
         echo "DATE_TAG_SUFFIX=$(date -u +"%Y-%m-%d-%H:%M:%S")" >> $GITHUB_ENV
      - name: test
        run: |
          echo "${{ env.DATE_TAG_SUFFIX }}"
        



